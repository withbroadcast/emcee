defmodule Emcee.Chain.Combine do
  @moduledoc """
  Given a set of documents, combine them into a single document.
  """

  # alias Emcee.Model

  def new(opts) do
    opts
  end

  def run(_model, _opts) do
    {:ok, "Combine"}
  end
end
